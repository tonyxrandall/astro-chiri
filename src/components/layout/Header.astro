---
import { getEntry } from 'astro:content'
import { themeConfig } from '@/config'
import ThemeToggle from '@/components/ui/ThemeToggle.astro'

const navigation = await getEntry('settings', 'navigation')
const siteName = navigation?.data.siteName ?? themeConfig.site.title
const navLinks = navigation?.data.navLinks ?? []
const ctaText = navigation?.data.ctaText
const ctaUrl = navigation?.data.ctaUrl
---

<header>
  <nav>
    <a class="site-name" href="/" aria-label={siteName}>
      <img src="/images/b-side-logo.png" alt="" width="28" height="28" />
      <span>{siteName}</span>
    </a>
    <div class="nav-links">
      {navLinks.map((link) => <a href={link.url}>{link.label}</a>)}
      {ctaText && ctaUrl && <a href={ctaUrl}>{ctaText}</a>}
      {themeConfig.general.themeToggle && <ThemeToggle />}
    </div>
  </nav>
</header>
<style>
  header {
    margin: 0 0 2rem;
  }

  nav {
    align-items: center;
    display: flex;
    gap: 1rem;
    justify-content: space-between;
  }

  a {
    color: var(--text-primary);
    text-decoration: none;
  }

  .site-name {
    align-items: center;
    display: inline-flex;
    font-weight: var(--font-weight-bold);
    gap: 0.5rem;
    min-width: 3rem;
  }

  .site-name img {
    border-radius: 0.35rem;
    display: block;
    height: 1.75rem;
    width: 1.75rem;
  }

  .nav-links {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: flex-end;
  }

  .nav-links a {
    color: var(--text-secondary);
    font-size: var(--font-size-s);
  }

  .nav-links a:hover {
    color: var(--text-primary);
  }
</style>
